# AbstractEnvironment
## 已实现接口
* 接口1
* 接口2
## 思考
* 问题1？
* 问题2？
## 属性
### ***IGNORE_GETENV_PROPERTY_NAME***
>配置在 spring.properties 是否允许在应用运行时读取系统环境变量
### ***ACTIVE_PROFILES_PROPERTY_NAME***
>记录在spring.properties 当前活跃的环境变量的key
### ***DEFAULT_PROFILES_PROPERTY_NAME***
>记录在spring.properties 默认配置的环境变量的key,
### ***RESERVED_DEFAULT_PROFILE_NAME***
>默认的环境变量的 value 的默认值
### activeProfiles
>当前活跃的环境变量列表
### defaultProfiles
>当前默认的环境变量列表
### propertySources
>实际存储keyvalue 的集合,每个propertySources 类似一个 entry 对象
### propertyResolver
### 属性
> 属性描述

## 方法

### 方法1
* 参数1:参数1描述
* 参数2:参数2描述
> 方法描述

#### 补充